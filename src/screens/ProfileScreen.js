import React from 'react';
import { connect } from 'react-redux';
import { AsyncStorage, StyleSheet, Text, SafeAreaView, Image } from 'react-native';
import logo from '../assets/logo.png';

let ProfileScreen= () => {

  return(
    <SafeAreaView>
      <Text>
        Profile Screen
      </Text>
    </SafeAreaView>
  )}

export default ProfileScreen;